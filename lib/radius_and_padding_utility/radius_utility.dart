import 'package:flutter/widgets.dart';

class RadiusUtility {
  static final defaultRaidus = BorderRadius.circular(12);
  static const appBarRaidus = Radius.circular(26);

  static final minradius = BorderRadius.circular(8);
}
